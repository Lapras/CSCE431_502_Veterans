
<h1>Users</h1>

<%= link_to "New user", new_admin_user_path, class: "action-button" %>
<%= link_to "Membership Requests", admin_membership_requests_path, class: "action-button" %>
<%= link_to "Discipline User", new_admin_discipline_record_path, class: "action-button" %>

<table>
  <thead>
    <tr>
      <th>Full Name</th>
      <th>Email</th>
      <th>Role</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <% next if user.roles.empty? || user.has_role?(:requesting) %>
      <tr>
        <td><%= user.full_name %></td>
        <td><%= user.email %></td>
        <td><%= user.roles.pluck(:name).join(", ") %></td>
        <td>
          <%= link_to "Show", [:admin, user] %>
          <%= link_to "Edit", edit_admin_user_path(user) %>
          <%= link_to "Delete", confirm_delete_admin_user_path(user) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>